// src/data/chestCommands.js
// Educational data for each Redis command unlocked from chests

export const CHEST_COMMAND_DATA = {
  SET: {
    command: 'SET',
    category: 'String',
    description: 'Sets a string value to a key.',
    syntax: 'SET key value [EX seconds] [PX milliseconds] [NX|XX] [KEEPTTL]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The key name to set' },
      { name: 'value', type: 'string', required: true, description: 'The string value to store' },
      { name: 'EX', type: 'seconds', required: false, description: 'Set expiry in seconds' },
      { name: 'PX', type: 'milliseconds', required: false, description: 'Set expiry in milliseconds' },
      { name: 'NX', type: 'flag', required: false, description: 'Only set if key does not exist' },
      { name: 'XX', type: 'flag', required: false, description: 'Only set if key already exists' },
      { name: 'KEEPTTL', type: 'flag', required: false, description: 'Retain existing TTL' },
    ],
    useCase: 'SET is the fundamental Redis command for storing simple key-value data. Use it for caching user sessions, configuration flags, or any simple string data. Example: SET session:abc "user_data" EX 3600 caches a session for 1 hour.',
    realWorld: 'Perfect for caching API responses, storing user preferences, or feature flags! In a real app: SET feature:dark-mode "enabled" EX 86400 keeps a feature flag for 24 hours.',
    example: 'SET user:100:name "Alex"',
    exampleExplanation: 'Creates a key "user:100:name" with value "Alex"',
  },
  GET: {
    command: 'GET',
    category: 'String',
    description: 'Gets the string value of a key.',
    syntax: 'GET key',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The key name to retrieve' },
    ],
    useCase: 'GET retrieves a value previously stored with SET. Returns nil if the key doesn\'t exist. This is your primary read operation for string data.',
    realWorld: 'Essential for reading cached data, session data, or configuration! Example: GET session:abc returns the user session data or nil if expired.',
    example: 'GET user:100:name',
    exampleExplanation: 'Returns "Alex" (the value we set earlier)',
  },
  DEL: {
    command: 'DEL',
    category: 'Key',
    description: 'Deletes one or more keys.',
    syntax: 'DEL key [key ...]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'Key(s) to delete' },
    ],
    useCase: 'DEL removes keys entirely from Redis. Use it to clean up expired sessions, invalidate caches, or remove temporary data. Returns the number of keys actually deleted.',
    realWorld: 'Critical for cache invalidation! When a user updates their profile: DEL user:100:cache forces a fresh read on next request. Also used in DEL goblin:shield to defeat enemies!',
    example: 'DEL user:100:name',
    exampleExplanation: 'Deletes the key "user:100:name" and returns 1',
  },
  HSET: {
    command: 'HSET',
    category: 'Hash',
    description: 'Sets field(s) in a hash stored at key.',
    syntax: 'HSET key field value [field value ...]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The hash key name' },
      { name: 'field', type: 'string', required: true, description: 'Field name within the hash' },
      { name: 'value', type: 'string', required: true, description: 'Value for the field' },
    ],
    useCase: 'HSET creates or updates fields in a Redis Hash. Hashes are perfect for storing objects with multiple fields (like user profiles, product info, or game entities) under a single key. Much more memory-efficient than separate keys!',
    realWorld: 'HSET is ideal for storing object properties like user profiles! Example: HSET user:100 name "Alex" email "alex@example.com" level 10 hp 100 stores a complete player profile in one key.',
    example: 'HSET user:100 name "Alex" class "Mage" level 10',
    exampleExplanation: 'Creates a hash at "user:100" with three fields',
  },
  HGET: {
    command: 'HGET',
    category: 'Hash',
    description: 'Gets the value of a hash field.',
    syntax: 'HGET key field',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The hash key name' },
      { name: 'field', type: 'string', required: true, description: 'Field name to retrieve' },
    ],
    useCase: 'HGET reads a single field from a hash. Use it when you only need one piece of data from an object without fetching the entire thing. Returns nil if key or field doesn\'t exist.',
    realWorld: 'Perfect for reading just one profile field! Example: HGET user:100 hp returns just the health value without loading the entire player object.',
    example: 'HGET user:100 class',
    exampleExplanation: 'Returns "Mage" (just the class field)',
  },
  HDEL: {
    command: 'HDEL',
    category: 'Hash',
    description: 'Deletes one or more hash fields.',
    syntax: 'HDEL key field [field ...]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The hash key name' },
      { name: 'field', type: 'string', required: true, description: 'Field(s) to delete' },
    ],
    useCase: 'HDEL removes specific fields from a hash while keeping the rest intact. Useful for unsetting optional fields or cleaning up deprecated data. Returns the number of fields actually deleted.',
    realWorld: 'Great for removing optional or deprecated fields! Example: HDEL user:100 deprecated_field cleans up old data without touching the rest of the profile.',
    example: 'HDEL user:100 class',
    exampleExplanation: 'Removes the "class" field from the hash',
  },
  LPUSH: {
    command: 'LPUSH',
    category: 'List',
    description: 'Prepends one or more values to a list.',
    syntax: 'LPUSH key value [value ...]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The list key name' },
      { name: 'value', type: 'string', required: true, description: 'Value(s) to prepend' },
    ],
    useCase: 'LPUSH adds elements to the LEFT (head) of a list. Lists maintain insertion order and are perfect for queues, stacks, recent activity feeds, or job queues. Use with RPOP for a queue, or LPOP for a stack!',
    realWorld: 'LPUSH + RPOP = Queue! Perfect for job queues: LPUSH queue:jobs "task1" adds a job, workers use RPOP to process. Also great for "recent items" feeds!',
    example: 'LPUSH queue:jobs "task1" "task2"',
    exampleExplanation: 'Prepends "task1" then "task2" to the list (task2 is now at head)',
  },
  RPOP: {
    command: 'RPOP',
    category: 'List',
    description: 'Removes and returns the last element of a list.',
    syntax: 'RPOP key [count]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The list key name' },
      { name: 'count', type: 'integer', required: false, description: 'Number of elements to pop (Redis 6.2+)' },
    ],
    useCase: 'RPOP removes and returns the RIGHTMOST (tail) element. Combined with LPUSH, this creates a FIFO queue! Workers pop jobs from the right while producers push to the left.',
    realWorld: 'The consumer side of a job queue! Example: RPOP queue:jobs returns and removes the oldest job. With count: RPOP queue:jobs 5 gets 5 jobs at once for batch processing.',
    example: 'RPOP queue:jobs',
    exampleExplanation: 'Returns and removes the last element ("task1")',
  },
  ZADD: {
    command: 'ZADD',
    category: 'Sorted Set',
    description: 'Adds member(s) with scores to a sorted set.',
    syntax: 'ZADD key score member [score member ...] [NX|XX] [CH] [INCR]',
    parameters: [
      { name: 'key', type: 'string', required: true, description: 'The sorted set key name' },
      { name: 'score', type: 'number', required: true, description: 'Numeric score for ordering' },
      { name: 'member', type: 'string', required: true, description: 'Unique member identifier' },
      { name: 'NX', type: 'flag', required: false, description: 'Only add new members' },
      { name: 'XX', type: 'flag', required: false, description: 'Only update existing members' },
      { name: 'CH', type: 'flag', required: false, description: 'Return changed count instead of added' },
      { name: 'INCR', type: 'flag', required: false, description: 'Increment score by given amount' },
    ],
    useCase: 'ZADD adds members to a sorted set with a numeric score. Members are automatically sorted by score! Perfect for leaderboards, ranking systems, priority queues, or time-series data. Use ZRANGE to get top/bottom entries.',
    realWorld: 'ZADD is THE command for leaderboards and rankings! Example: ZADD highscores 1000 "Alex" 950 "Sam" 1200 "Jordan" creates a ranked leaderboard. Get top 10: ZRANGE highscores 0 9 REV!',
    example: 'ZADD arena:scores 100 "Hero" 200 "Champion"',
    exampleExplanation: 'Adds "Hero" with score 100 and "Champion" with score 200',
  },
  PUBLISH: {
    command: 'PUBLISH',
    category: 'Pub/Sub',
    description: 'Posts a message to a channel.',
    syntax: 'PUBLISH channel message',
    parameters: [
      { name: 'channel', type: 'string', required: true, description: 'Channel name to publish to' },
      { name: 'message', type: 'string', required: true, description: 'Message payload to send' },
    ],
    useCase: 'PUBLISH sends a message to all subscribers of a channel. It\'s fire-and-forget - no history is kept. Perfect for real-time notifications, chat messages, or event broadcasting between services.',
    realWorld: 'Real-time messaging backbone! Example: PUBLISH notifications:user:100 "You have a new message" instantly delivers to all subscribed clients. Used for chat, live updates, microservice events!',
    example: 'PUBLISH news:updates "Redis 7.2 released!"',
    exampleExplanation: 'Broadcasts the message to all "news:updates" subscribers',
  },
  SUBSCRIBE: {
    command: 'SUBSCRIBE',
    category: 'Pub/Sub',
    description: 'Listens for messages on channel(s).',
    syntax: 'SUBSCRIBE channel [channel ...]',
    parameters: [
      { name: 'channel', type: 'string', required: true, description: 'Channel name(s) to subscribe to' },
    ],
    useCase: 'SUBSCRIBE creates a persistent connection listening for messages on channel(s). The connection stays open and receives messages in real-time. Use PSUBSCRIBE for pattern-based subscriptions (e.g., news:*).',
    realWorld: 'The listener side of real-time systems! Example: SUBSCRIBE news:updates keeps a connection open to receive all news broadcasts. Client libraries handle the subscription loop automatically.',
    example: 'SUBSCRIBE news:updates',
    exampleExplanation: 'Opens a subscription to the "news:updates" channel',
  },
  CLUSTER: {
    command: 'CLUSTER',
    category: 'Cluster',
    description: 'Manages Redis Cluster operations.',
    syntax: 'CLUSTER subcommand [args...]',
    parameters: [
      { name: 'MEET', type: 'subcommand', required: false, description: 'CLUSTER MEET ip port - adds a node to cluster' },
      { name: 'NODES', type: 'subcommand', required: false, description: 'CLUSTER NODES - shows all cluster nodes' },
      { name: 'KEYSLOT', type: 'subcommand', required: false, description: 'CLUSTER KEYSLOT key - shows which slot a key maps to' },
      { name: 'ADDSLOTS', type: 'subcommand', required: false, description: 'CLUSTER ADDSLOTS slot... - assigns slots to current node' },
    ],
    useCase: 'CLUSTER is the management interface for Redis Cluster. It handles node membership, slot assignment, failover, and monitoring. Redis Cluster shards data across nodes by hash slots (0-16383) for horizontal scaling.',
    realWorld: 'Essential for running Redis at scale! Example: CLUSTER MEET 10.0.0.2 6379 adds a new node. CLUSTER KEYSLOT user:100 tells you which node owns that key. CLUSTER NODES shows cluster health.',
    example: 'CLUSTER MEET 10.0.0.2 6379',
    exampleExplanation: 'Adds a new node at 10.0.0.2:6379 to the cluster',
  },
}

export function getChestCommandData(gem) {
  return CHEST_COMMAND_DATA[gem] || CHEST_COMMAND_DATA.SET
}